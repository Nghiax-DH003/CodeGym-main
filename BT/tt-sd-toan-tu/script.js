// Khai báo biến
let inputWidth;
let inputHeight;

// Lấy dữ liệu từ người dùng
inputWidth = prompt("Enter the width");
inputHeight = prompt("Enter the height");

// In ra console (hoặc bạn có thể xử lý tiếp)
console.log("Width = " + inputWidth);
console.log("Height = " + inputHeight);

document.write("Width = " + inputWidth + "<br/>");
document.write("Height = " + inputHeight + "<br/>");
